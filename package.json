{
  "name": "poker-tactics-player-draft",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "python3 -m http.server 4173",
    "check": "node --check app.js",
    "build": "mkdir -p dist && cp index.html app.js styles.css dist/ && cp -R assets dist/assets && cp manifest.webmanifest dist/ && cp _headers dist/ && cp .nojekyll dist/"
  }
}
